import styled from "styled-components";

export const Container = styled.h1`
    font-weight: 700;
    font-size: 2.4rem;
    color: ${({ theme }) => theme.COLORS.PINK};
    border: none;
    background: none;
`;